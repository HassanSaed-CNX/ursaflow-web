import { PlaceholderTemplate } from '@/components/PlaceholderTemplate';
import { Package } from 'lucide-react';

export function Kitting() {
  return (
    <PlaceholderTemplate
      icon={Package}
      titleKey="pages.kitting"
      breadcrumbLabel="Kitting"
    />
  );
}
