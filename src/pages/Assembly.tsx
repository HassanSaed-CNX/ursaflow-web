import { PlaceholderTemplate } from '@/components/PlaceholderTemplate';
import { Wrench } from 'lucide-react';

export function Assembly() {
  return (
    <PlaceholderTemplate
      icon={Wrench}
      titleKey="pages.assembly"
      breadcrumbLabel="Assembly"
    />
  );
}
